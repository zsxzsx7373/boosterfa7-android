workflows:
  android-build:
    name: BoosterFA7 Signed APK
    max_build_duration: 15
    environment:
      android_signing:
        - boosterfa7_release_keystore
      vars:
        JAVA_VERSION: 11
        CM_KEY_ALIAS: boosterfa7
    scripts:
      - ./gradlew assembleRelease
    artifacts:
      - app/build/outputs/**/*.apk
    publishing:
      email:
        recipients:
          - npo.fa@outlook.com